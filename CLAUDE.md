# 黑马记账 — 产品文档

## 项目概述

- **项目名称：** 黑马记账
- **项目类型：** 跨平台桌面记账应用
- **运行平台：** Windows 10/11 和 macOS
- **目标用户：** 个人用户，用于日常开支的记录与管理
- **核心目标：** 简单、直观、美观的个人记账工具

---

## 技术栈

| 技术 | 作用 | 通俗解释 |
|------|------|---------|
| React 18 | 前端 UI 框架 | 用来画界面、做交互的"画笔" |
| TypeScript 5 | 编程语言 | 带"类型检查"的 JavaScript，减少出错 |
| Ant Design 5 | UI 组件库 | 现成的漂亮"按钮、输入框、表格"零件 |
| Vite 5 | 前端构建工具 | 把代码"打包"成浏览器能运行的格式 |
| Node.js + Express | 后端服务器 | 处理数据存储和读取请求 |
| sql.js | 数据库 | 一个小型数据库，数据存在电脑本地文件 |
| Electron | 桌面窗口 | 未来打包成独立桌面应用的容器（当前先以浏览器方式运行） |

---

## 核心功能

### 1. 账单记录（核心）
- 记录一笔花销的：**金额（人民币）、日期、分类、备注**
- 金额单位为"元"，支持小数点后两位
- 日期默认为当天，用户可手动修改
- 备注为可选填项

### 2. 两级支出分类

| 一级大类 | 二级小类 |
|---------|---------|
| 🍽️ 餐饮 | 早餐、午餐、晚餐、零食、饮品、外卖、聚餐 |
| 🚗 交通 | 公交、地铁、打车、加油、停车、火车/高铁、飞机 |
| 🛒 购物 | 日用品、数码产品、家居用品、书籍、美妆护肤 |
| 🏠 居住 | 房租、水电、物业、维修、日用品 |
| 🎮 娱乐 | 电影、游戏、旅游、运动健身、KTV/酒吧 |
| 🏥 医疗 | 门诊、药品、体检、住院 |
| 📚 教育 | 培训、书籍、考试报名、学费 |
| 📱 通讯 | 话费、网费、快递 |
| 👗 服饰 | 衣服、鞋子、包包、配饰 |
| 📦 其他 | 人情往来、宠物、投资理财、其他杂项 |

### 3. 账单列表
- 按时间倒序显示所有账单
- 每条显示：日期、分类、金额、备注
- 支持按一级分类筛选
- 显示总笔数和总支出金额

### 4. 统计功能
- 按一级分类汇总统计
- 显示每类的笔数、总金额、占比

### 5. 扩展功能（后续版本）
- 月度统计图表
- 收入记录
- 预算管理
- 数据导出（Excel）
- Electron 打包为独立桌面应用

---

## 用户交互准则（非常重要！）

> **用户是编程零基础小白。在整个项目开发过程中，以下准则必须严格遵守：**

1. **任何技术相关的决策，Claude 必须先列出 2-3 个方案**
2. **每个方案必须用通俗语言解释：**
   - 这个方案是什么（用生活化比喻）
   - 优点是什么
   - 缺点是什么
   - 适合什么场景
3. **由用户来做最终选择，Claude 不能自行决定技术方案**
4. **不能假设用户懂任何技术术语**，如需使用术语，必须先解释
5. **每次沟通都要简洁明了**，避免大段技术性描述
6. **功能实现后主动展示结果**，让用户确认

---

## 项目结构

```
黑马记账app/
├── CLAUDE.md                  # 本文件，产品文档
├── package.json               # 项目依赖配置
├── index.html                 # HTML 入口
├── vite.config.ts             # Vite 构建配置
├── tsconfig.json              # TypeScript 配置（前端）
├── start.ts                   # 启动脚本（一键启动服务器+浏览器）
├── server/                    # 后端服务器
│   ├── server.ts              # Express 服务器（API + 静态文件）
│   └── database.ts            # SQLite 数据库操作
├── src/                       # React 前端代码
│   ├── App.tsx                # 应用根组件
│   ├── main.tsx               # 前端入口
│   ├── api/                   # API 客户端
│   │   └── index.ts           # 与后端通信的接口
│   ├── pages/                 # 页面组件
│   │   ├── AddBill.tsx        # 添加账单页
│   │   ├── BillList.tsx       # 账单列表页
│   │   └── Stats.tsx          # 统计页
│   ├── components/            # 通用组件
│   │   └── CategoryPicker.tsx # 两级分类选择器
│   ├── data/                  # 数据定义
│   │   └── categories.ts      # 分类数据
│   ├── types/                 # TypeScript 类型定义
│   └── styles/                # 样式文件
│       └── global.css
└── data/                      # 运行时数据
    └── heimaa-accounting.db   # SQLite 数据库文件
```

---

## 架构说明

### 当前架构（浏览器模式）
```
┌─────────────┐     HTTP      ┌──────────────┐     SQL     ┌──────────┐
│   浏览器     │ ◄──────────► │  Express 服务器 │ ◄─────────► │  SQLite  │
│ (React UI)  │               │  (Node.js)    │             │  数据库   │
└─────────────┘               └──────────────┘             └──────────┘
```

- **React 前端**：在浏览器中运行，负责界面展示
- **Express 服务器**：在本地运行，处理数据请求，把结果返回给前端
- **SQLite 数据库**：一个文件（data/heimaa-accounting.db），存储所有记账数据

### 启动方式
```bash
# 方式1：一键启动（推荐）
npm start

# 方式2：仅启动服务器（手动打开浏览器访问 http://localhost:3456）
npm run server
```

---

## 数据存储设计

### 账单表 (bills)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 自增主键 |
| amount | REAL | 金额（元） |
| date | TEXT | 日期 (YYYY-MM-DD) |
| category_l1 | TEXT | 一级分类 |
| category_l2 | TEXT | 二级分类 |
| note | TEXT | 备注（可选） |
| created_at | TEXT | 创建时间 |

---

## 开发日志

- **2026-07-23**：项目启动，创建产品文档，用户选择 Electron 技术栈
- **2026-07-24**：因国内网络限制（所有 Electron 镜像缺少关键文件 electron.asar），调整为浏览器 + Express 服务器架构。完成所有核心功能：添加账单、账单列表、分类筛选、统计、两级分类联动选择。应用成功运行。
